
square <- function(x) {
  return(x*x)
}

square 
<- 
function 
( 
x 
) 
{ 
return
(
x
*
x
) 
}

cube <- 
	function ( x = 5 )
{
  return(x*x*x);
}

powers<-function(x) {
  parcel = list(x2=x*x, x3=x*x*x, x4=x*x*x*x);
  return(parcel);
}

powerful <- function(x) {
  return(list(x2=x*x, x3=x*x*x, x4=x*x*x*x));
}

	  powerful <- function(x) {list(x2=x*x, x3=x*x*x, x4=x*x*x*x)}

x <- 1  # This is a comment...
y <- "  #... but this is not."
z <- "  # powerful <- function(x) {list(x2=x*x, x3=x*x*x, x4=x*x*x*x)}"

